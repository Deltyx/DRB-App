import Chat from "../../components/Chat/Chat"

import './Lobby.scss';

export default function Lobby() {
    return (
        <section className="Lobby-container">
            <Chat />
        </section>
    )
}